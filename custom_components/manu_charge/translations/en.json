{
  "config": {
    "step": {
      "user": {
        "title": "Manual Charge",
        "description": "Select the entities of your inverter.",
        "data": {
          "charge_current_entity": "Battery max charging current entity",
          "battery_soc_entity": "Battery state of charge sensor",
          "notifications": "Show persistent notifications"
        }
      }
    },
    "abort": {
      "already_configured": "Manual Charge is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Manual Charge",
        "data": {
          "charge_current_entity": "Battery max charging current entity",
          "battery_soc_entity": "Battery state of charge sensor",
          "notifications": "Show persistent notifications"
        }
      }
    }
  },
  "entity": {
    "switch": {
      "master_switch": { "name": "Schedule master switch" },
      "force_now": { "name": "Force block now" },
      "range1_enable": { "name": "Range 1 enabled" },
      "range2_enable": { "name": "Range 2 enabled" }
    },
    "number": {
      "range1_soc": { "name": "Range 1 minimum SOC" },
      "range2_soc": { "name": "Range 2 minimum SOC" },
      "preset_current": { "name": "Fallback current" },
      "saved_current": { "name": "Saved current" }
    },
    "time": {
      "range1_on": { "name": "Range 1 start" },
      "range1_off": { "name": "Range 1 end" },
      "range2_on": { "name": "Range 2 start" },
      "range2_off": { "name": "Range 2 end" }
    },
    "binary_sensor": {
      "blocking_active": { "name": "Blocking active" }
    }
  }
}
